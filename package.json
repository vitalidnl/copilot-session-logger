{
  "name": "copilot-session-logger",
  "version": "0.1.0",
  "description": "Local-only MCP server providing save-copilot-session tool to persist Copilot chat sessions using a workspace template.",
  "private": true,
  "type": "module",
  "bin": {
    "copilot-session-logger": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "node --loader ts-node/esm src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "ts-node": "^10.9.2",
    "@types/node": "^22.10.2"
  }
}
